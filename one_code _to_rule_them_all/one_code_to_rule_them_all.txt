

detect particles
→ manually fix missing ones
→ track position + velocity + rotation
→ predict next position
→ if missing, search locally with special close-up model
→ recover particle
→ continue tracking


ParticleTrack object
    id
    x, y
    theta
    vx, vy
    omega
    missed_frames
    history


1. Run normal YOLO model on full frame
2. Predict where every existing particle should be
3. Match YOLO detections to predicted particles
4. Update matched particles
5. For unmatched particles:
       search predicted local ROI with close-up low-confidence model
6. If recovered:
       update particle
   else:
       mark as missing
7. Save all particle states to dataframe